import katex from 'katex'
import 'katex/dist/katex.min.css'

// Renderiza fórmulas matemáticas en texto usando KaTeX
// Escapa $ antes de números para evitar conflictos con formato moneda
// TODO: implementar parser completo con soporte inline ($...$) y display ($$...$$)

export function renderLatex(texto) {
  return texto
}
